<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="false" use-expressions="true">
        <intercept-url pattern="/js/**" access="permitAll()" />
        <intercept-url pattern="/css/**" access="permitAll()" />
        <intercept-url pattern="/assets/**" access="permitAll()" />
        <intercept-url pattern="/image/**" access="permitAll()" />
        <intercept-url pattern="/login" access="permitAll()" />
        <csrf />
        <remember-me remember-me-cookie="remember-me"
                     token-validity-seconds="1000000" />
        <http-basic />
        <form-login  login-page="/login"
                     login-processing-url="/login"
                     default-target-url="/home"
                     authentication-failure-url="/login?error"
                     username-parameter="username"
                     password-parameter="password"/>
        <intercept-url pattern="/**" access="isAuthenticated()" />
        <!--<session-management invalid-session-url="/session_timeout" />-->

        <logout delete-cookies="JSESSIONID" logout-url="/logout" logout-success-url="/login" />
    </http>

    <authentication-manager erase-credentials="false">
        <authentication-provider user-service-ref="userDetailsServiceImpl" />
        <!--<authentication-provider>-->
            <!--<user-service>-->
                <!--<user name="sidney" password="sidney" authorities="ROLE_USER" />-->
            <!--</user-service>-->
        <!--</authentication-provider>-->

    </authentication-manager>

    <b:bean id="userDetailsServiceImpl" class="com.tinthon.coa.service.UserDetailsServiceImpl" />


</b:beans>